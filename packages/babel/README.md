# babel-plugin-july

This is a babel plugin to compile July to JavaScript.

## Usage

```
npm install -D babel-plugin-july
```

then update `.babelrc` 

```json
{
  "plugins": [
    "babel-plugin-july"
  ]
}
```